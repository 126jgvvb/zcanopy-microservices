info:
  name: Get Booking by Code
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/bookings/code/{{txnCode}}"
  auth: inherit

runtime:
  variables:
    - name: txnCode
      value: "12345677"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
