info:
  name: Get Property Clients
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/listings/get_property_clients?propertyId=9"
  params:
    - name: propertyId
      value: "9"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
