info:
  name: Get Property Details for Customer
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/customer/properties/details?propertyId=9"
  params:
    - name: propertyId
      value: "9"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
