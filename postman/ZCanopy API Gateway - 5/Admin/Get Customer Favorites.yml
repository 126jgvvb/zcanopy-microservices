info:
  name: Get Customer Favorites
  type: http
  seq: 32

http:
  method: GET
  url: "{{baseUrl}}/admin/customers/{{customerId}}/favorites"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
