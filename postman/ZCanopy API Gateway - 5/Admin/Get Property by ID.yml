info:
  name: Get Property by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/admin/properties/prop-123"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
