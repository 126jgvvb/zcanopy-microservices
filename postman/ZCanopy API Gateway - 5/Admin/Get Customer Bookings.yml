info:
  name: Get Customer Bookings
  type: http
  seq: 31

http:
  method: GET
  url: "{{baseUrl}}/admin/customers/{{customerId}}/bookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
