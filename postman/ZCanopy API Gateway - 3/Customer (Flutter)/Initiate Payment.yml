info:
  name: Initiate Payment
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/payment/initiate_payment"
  body:
    type: json
    data: |-
      {
        "phoneNumber": "0700000000",
        "amount": 10000,
        "userId": "user-id-123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
