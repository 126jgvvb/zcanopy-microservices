info:
  name: Login Customer
  type: http
  seq: 21

http:
  method: POST
  url: "{{baseUrl}}/web/customer/login"
  body:
    type: json
    data: |-
      {
        "email": "wadikakevin@gmail.com",
        "password": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
