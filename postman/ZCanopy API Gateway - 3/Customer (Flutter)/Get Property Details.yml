info:
  name: Get Property Details
  type: http
  seq: 28

http:
  method: GET
  url: "{{baseUrl}}/web/customer/properties?propertyId=property-id-123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
