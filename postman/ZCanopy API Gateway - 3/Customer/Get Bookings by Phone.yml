info:
  name: Get Bookings by Phone
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/customer/bookings/phone"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
