info:
  name: Search Public Properties
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/web/public/search"
  headers:
    - name: x-session-id
      value: "{{webSessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
