info:
  name: Retrieve Customer Searches
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/web/customer/searches/retrieve"
  headers:
    - name: Content-Type
      value: application/json
    - name: x-session-id
      value: "{{webSessionId}}"
  body:
    type: json
    data: |-
      {
        "sessionToken": "{{customerSessionToken}}",
        "page": 1,
        "limit": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
