info:
  name: Get customer profile
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/api/web/customer/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
