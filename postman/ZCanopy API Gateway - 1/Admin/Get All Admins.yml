info:
  name: Get All Admins
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/admin/admins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
