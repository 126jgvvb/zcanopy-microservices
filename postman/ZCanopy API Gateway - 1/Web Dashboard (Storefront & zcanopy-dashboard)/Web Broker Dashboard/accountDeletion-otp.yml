info:
  name: accountDeletion-otp
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/web/broker/account/delete/request-otp"
  headers:
    - name: Authorization
      value: Bearer   {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
