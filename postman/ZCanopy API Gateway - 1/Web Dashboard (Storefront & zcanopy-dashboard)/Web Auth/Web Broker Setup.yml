info:
  name: Web Broker Setup
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/web/auth/broker/setup"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "brokerCode": "75874748",
        "password": "password123",
        "deviceId": "web-dashboard",
        "brokerBrandName": "Acme Realty"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
