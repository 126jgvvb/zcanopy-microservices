info:
  name: Get Public Property Details
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/web/public/properties/prop-123"
  headers:
    - name: x-session-id
      value: "{{webSessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
