info:
  name: Get Property Comments
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/web/customer/properties/p1/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
