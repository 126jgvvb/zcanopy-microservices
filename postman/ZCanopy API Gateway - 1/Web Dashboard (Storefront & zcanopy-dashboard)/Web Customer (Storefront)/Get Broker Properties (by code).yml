info:
  name: Get Broker Properties (by code)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/web/customer/broker/BROKER123/properties"
  headers:
    - name: x-session-id
      value: "{{webSessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
