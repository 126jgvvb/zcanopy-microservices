info:
  name: Broker Setup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/broker/setup"
  body:
    type: json
    data: |-
      {
        "brokerCode": "75874748",
        "password": "password123",
        "deviceId": "device-001",
        "brokerBrandName": "Delos Apartments"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
