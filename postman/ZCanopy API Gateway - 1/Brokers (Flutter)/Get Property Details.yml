info:
  name: Get Property Details
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/listings/get-item-details"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
