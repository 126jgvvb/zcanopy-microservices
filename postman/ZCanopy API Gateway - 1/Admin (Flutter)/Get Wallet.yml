info:
  name: Get Wallet
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/admin/wallet"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
