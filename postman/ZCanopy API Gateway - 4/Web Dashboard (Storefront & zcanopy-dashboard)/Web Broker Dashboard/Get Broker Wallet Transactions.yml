info:
  name: Get Broker Wallet Transactions
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/web/broker/wallet/transactions"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
