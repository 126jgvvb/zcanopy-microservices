info:
  name: Retrieve Booking by Code
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/customer/bookings/retrieve"
  body:
    type: json
    data: |-
      {
        "code": "CODE123",
        "phoneNumber": "0700000000"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
