info:
  name: Search Properties (Customer App)
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/web/customer/search"
  headers:
    - name: Authorization
      value: Bearer {{customerAccessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
