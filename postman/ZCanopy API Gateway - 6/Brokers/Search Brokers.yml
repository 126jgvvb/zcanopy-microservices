info:
  name: Search Brokers
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/brokers/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
