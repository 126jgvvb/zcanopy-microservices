info:
  name: Get Broker Bookings
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/bookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
