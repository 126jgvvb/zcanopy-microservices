info:
  name: email verification code
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/gate-way/get-change-passsword-otp"
  body:
    type: json
    data: |-
      {
        "email": "wadikakevin@gmail.com",
        "phoneNumber": "0700000000"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
