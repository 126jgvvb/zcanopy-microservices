info:
  name: Delete Broker Property
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/web/broker/properties/prop-123"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
