info:
  name: List Public Properties
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/web/public/properties"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
