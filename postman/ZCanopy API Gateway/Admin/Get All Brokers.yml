info:
  name: Get All Brokers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/admin/brokers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
