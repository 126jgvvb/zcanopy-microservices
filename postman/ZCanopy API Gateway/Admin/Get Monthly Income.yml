info:
  name: Get Monthly Income
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/admin/income/monthly"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
