info:
  name: Get Property Details for Customer
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/customer/properties/prop-123/details"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
