info:
  name: Get User by Username
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/users/get-user-by-username"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
