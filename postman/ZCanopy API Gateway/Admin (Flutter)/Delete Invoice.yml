info:
  name: Delete Invoice
  type: http
  seq: 18

http:
  method: DELETE
  url: "{{baseUrl}}/admin/invoices/invoice-id-123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
