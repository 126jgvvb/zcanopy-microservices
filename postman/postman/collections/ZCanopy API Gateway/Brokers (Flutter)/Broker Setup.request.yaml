$kind: http-request
url: "{{baseUrl}}/auth/broker/setup"
method: POST
body:
  type: json
  content: |-
    {
      "brokerCode": "BROKER123",
      "password": "password123",
      "deviceId": "device-001",
      "brokerBrandName": "My Broker Brand"
    }
order: 1000
