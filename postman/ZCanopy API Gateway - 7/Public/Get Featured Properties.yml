info:
  name: Get Featured Properties
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/public/properties/featured"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
