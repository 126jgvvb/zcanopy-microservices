info:
  name: Search Properties (with Geo)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/web/customer/search"
  headers:
    - name: Authorization
      value: Bearer {{customerAccessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
