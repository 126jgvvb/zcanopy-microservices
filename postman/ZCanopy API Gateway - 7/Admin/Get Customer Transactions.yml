info:
  name: Get Customer Transactions
  type: http
  seq: 29

http:
  method: GET
  url: "{{baseUrl}}/admin/customers/{{customerId}}/transactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
