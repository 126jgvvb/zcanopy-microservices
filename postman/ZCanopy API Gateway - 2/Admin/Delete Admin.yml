info:
  name: Delete Admin
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{baseUrl}}/admin/admins/admin-id-123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
