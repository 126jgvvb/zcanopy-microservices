info:
  name: Get Properties
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/properties"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
