info:
  name: Resolve Location Name
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/properties/resolve-location-name"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
